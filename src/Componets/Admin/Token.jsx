// import React from 'react'

// import React from 'react';
// import { useSelector } from 'react-redux';
// import useAxios from './useAxios'; // Assuming this is the path to your useAxios file

// const Token = () => {
//   const accessToken = useSelector(state => state.auth.token); // Retrieve token from Redux store
//   const refreshToken = useSelector(state => state.auth.refresh); // Retrieve token from Redux store

//   const axiosInstance = useAxios(accessToken); // Pass token to useAxios
//   const axiosInstance = useAxios(refreshToken); // Pass token to useAxios

//   // Use axiosInstance to make requests

//   return (
//     // Your component JSX
//   );
// }

// export default Token;
